
function makePalin(number) {
    return String(number).split("").reverse().join("")
}
function isPalin(number) {
    return Number(makePalin(number)) === number
}

function numericalPalin(num) {
    let array = []
    
    for (let i = 0; i < num.length; i++) {
        
    }
}